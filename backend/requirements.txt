# VTrackAI Studio Backend - GPU Dependencies
# Minimal requirements for Colab/GPU deployment

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Video Processing
opencv-python>=4.8.0

# Audio Processing (cross-platform, no torchcodec)
librosa>=0.10.0

# ML Framework
torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# Model Hub
huggingface_hub>=0.16.0

# Colab Deployment (optional)
pyngrok>=6.0.0
nest-asyncio>=1.5.0

# SAM3 (install separately)
# pip install -e ./sam3
